<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.select :building_id, current_user.company.buildings.map { |c| [c.name, c.id] } %>
  </div>
  <div>
    <%= f.attachment_field :room_image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
